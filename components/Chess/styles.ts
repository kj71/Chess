import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  row: {
    flexDirection: "row",
  },
  chessboardContainer: {
    borderWidth: 1,
    borderColor: "#000",
  },
});

export default styles;